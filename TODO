NEXT

- fix email preview text (rename to "teaser" maybe!)
- (todo) enable twitter, emails, etc.

NTH

- (support) support older browsers
  - drop calc() uses from everywhere

- (nth author) author compose,bio,email form graceful degradation
