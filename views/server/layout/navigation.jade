nav.nv-container
  if model.user
    div.nv-logon
      a.nv-logon-anchor(href='/account/logout') Logout
  else
    div.nv-logon
      a.nv-logon-anchor(href='/account/login') Login

  if model.user
    ul.nv-items
      li.nv-item
        a(href='/account/bio', aria-label='Edit your biography') Bio

      //- TODO: req.user should be expanded from an id to the actual user.
      //- if model.user.author
      li.nv-item
        a(href='/author/compose', aria-label='Prepare a fresh draft') Compose
      li.nv-item
        a(href='/author/review', aria-label='Review articles and drafts') Review
      li.nv-item
        a(href='/author/email', aria-label='Prepare and send out an email campaign') Email Campaign
