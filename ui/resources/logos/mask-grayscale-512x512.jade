extends _template
block template_vars
  -
    fallbackImage = '/mask-grayscale-512x512.png'
    options = {
      getFill: function (x, y, defaultColor, group) {
        return group === 'eye' ? '#ffffff' : '#000000';
      },
      width: 512,
      height: 512
    }
