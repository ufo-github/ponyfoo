- publication = issue.publication
if issue.status === 'draft'
  div Draft
else if issue.status === 'ready'
  if issue.statusReach === 'scheduled'
    div Slated for release
    div
      time(datetime=publication.datetime, aria-label='Publishing ' + publication.title)='on ' + publication.text
  else
    div Ready for release
else if issue.status === 'released'
  div
    span='Released to ' + issue.statusReach
  div
    span='Publication '
    time(datetime=publication.datetime, aria-label='Published ' + publication.title)='on ' + publication.text
